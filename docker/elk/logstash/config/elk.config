input {
    lumberjack {
        port => 5043
        ssl_certificate => /config-dir/lumberjack.crt
        ssl_key => /config-dir/lumberjack.key
    }
}
output {
    elasticsearch { host => localhost }
    stdout { }
}
